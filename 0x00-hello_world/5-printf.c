#include <stdio.h>

/**
 * main - uses printf to write to write output
 * return - returns 0 for success, anything else for error
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}

